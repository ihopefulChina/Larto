/// <reference types="vite/client" />

// `<webview>` JSX typing comes from @types/react (WebViewHTMLAttributes); refs are cast to
// Electron's `WebviewTag` where the webview API (loadURL, send, getWebContentsId…) is needed.
export {}
